// Professionelle App Initialisierung
document.addEventListener('DOMContentLoaded', () => {
    console.log('--- Web Dev Journey Started ---');
    console.log('Die Webseite wurde erfolgreich geladen!');
    
    const appContainer = document.getElementById('app');
    
    // Kleiner Willkommensgruß in der Konsole
    const welcomeMsg = 'Viel Erfolg beim Lernen, BlazeR!';
    console.info('%c' + welcomeMsg, 'color: #2c3e50; font-weight: bold; font-size: 1.2rem;');
});
